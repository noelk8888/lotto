'use client';

import { ChangeEvent, useEffect, useMemo, useRef, useState } from 'react';
import { Camera, CheckCircle2, ChevronRight, ExternalLink, RotateCcw, ScanLine, ShieldCheck, Ticket, Trophy, XCircle } from 'lucide-react';

const games = ['Lotto 6/42', 'Megalotto 6/45', 'Superlotto 6/49', 'Grand Lotto 6/55', 'Ultra Lotto 6/58', '6D Lotto', '4D Lotto', '3D Lotto', '2D Lotto'];
type CheckResult = { game: string; combination: string; date: string; jackpot: string; winners: string };
const numbers = (value: string) => value.match(/\d{1,2}/g)?.map(Number) ?? [];
const lineLabel = (value: string, index: number) => value.match(/^\s*([A-Z])\s*[:.-]/i)?.[1]?.toUpperCase() ?? `Line ${index + 1}`;
const gamePatterns = [
  ['ULTRALOTTO658', 'Ultra Lotto 6/58'], ['GRANDLOTTO655', 'Grand Lotto 6/55'], ['SUPERLOTTO649', 'Superlotto 6/49'], ['MEGALOTTO645', 'Megalotto 6/45'], ['LOTTO642', 'Lotto 6/42'], ['6DLOTTO', '6D Lotto'], ['4DLOTTO', '4D Lotto'], ['3DLOTTO', '3D Lotto'], ['2DLOTTO', '2D Lotto'],
] as const;
const months: Record<string, string> = { jan: '01', feb: '02', mar: '03', apr: '04', may: '05', jun: '06', jul: '07', aug: '08', sep: '09', oct: '10', nov: '11', dec: '12' };
const validEntry = /^\s*([A-D])\s*[:.-]\s*((?:\d{1,2}\s+){5}\d{1,2})\s*$/i;
function scanTicket(read: string) {
  const compact = read.toUpperCase().replace(/[^A-Z0-9]/g, '');
  const detectedGame = gamePatterns.find(([pattern]) => compact.includes(pattern))?.[1] ?? '';
  const draw = read.match(/DRAW[\s\S]{0,80}?(\d{1,2})\s*[-/]\s*([A-Z]{3})\s*[-/]\s*(\d{2,4})/i);
  const date = draw ? `${draw[3].length === 2 ? `20${draw[3]}` : draw[3]}-${months[draw[2].toLowerCase()] ?? ''}-${draw[1].padStart(2, '0')}` : '';
  const found = new Map<string, string>();
  for (const line of read.split('\n')) {
    const label = line.match(/^\s*['|]?\s*([A-D])\s*[:.)]?/i)?.[1]?.toUpperCase();
    const values = line.match(/\d{1,2}/g) ?? [];
    if (label && values.length >= 6 && !found.has(label)) found.set(label, `${label}: ${values.slice(0, 6).map((value) => value.padStart(2, '0')).join(' ')}`);
  }
  const entries = ['A', 'B', 'C', 'D'].map((label) => found.get(label)).filter((entry): entry is string => Boolean(entry));
  return { detectedGame, date, entries: entries.join('\n') };
}
function prizeTier(game: string, matched: number, total: number) {
  if (matched === total) return 'Jackpot winner';
  if (game.includes('6/') || game === '6D Lotto') { if (matched === 5) return '5-number prize tier'; if (matched === 4) return '4-number prize tier'; if (matched === 3) return '3-number prize tier'; }
  return matched > 0 ? `${matched} matching number${matched === 1 ? '' : 's'} — no prize` : 'No matching prize tier';
}

export default function Home() {
  const input = useRef<HTMLInputElement>(null);
  const [image, setImage] = useState<string | null>(null); const [game, setGame] = useState('Ultra Lotto 6/58'); const [date, setDate] = useState(''); const [lines, setLines] = useState(''); const [loading, setLoading] = useState(false); const [scanning, setScanning] = useState(false); const [result, setResult] = useState<CheckResult | null>(null); const [error, setError] = useState('');
  const ticketLines = useMemo(() => lines.split('\n').map((line) => line.trim()).filter((line) => validEntry.test(line)).slice(0, 4), [lines]);
  useEffect(() => {
    const context = (document as Document & { modelContext?: { registerTool: (tool: unknown, options: { signal: AbortSignal }) => void | Promise<void> } }).modelContext;
    if (!context?.registerTool) return;
    const lifecycle = new AbortController();
    void Promise.resolve(context.registerTool({ name: 'stage_lotto_ticket_check', title: 'Prepare lotto ticket check', description: 'Fill the visible ticket-check fields with confirmed game, draw date, and number lines before checking PCSO results.', inputSchema: { type: 'object', properties: { game: { type: 'string', enum: games }, date: { type: 'string', description: 'Draw date in YYYY-MM-DD format' }, lines: { type: 'string', description: 'One or more number lines, separated by new lines' } }, required: ['game', 'date', 'lines'], additionalProperties: false }, annotations: { readOnlyHint: false, untrustedContentHint: true }, execute: (value: unknown) => { const ticket = value as { game?: string; date?: string; lines?: string }; if (!games.includes(ticket.game ?? '') || !/^\d{4}-\d{2}-\d{2}$/.test(ticket.date ?? '') || !ticket.lines?.trim()) throw new Error('Provide a supported game, YYYY-MM-DD draw date, and at least one number line.'); setGame(ticket.game!); setDate(ticket.date!); setLines(ticket.lines!); return { staged: true, game: ticket.game, date: ticket.date, lineCount: ticket.lines.split(/\n|,/).filter(Boolean).length }; } }, { signal: lifecycle.signal })).catch(() => undefined);
    return () => lifecycle.abort();
  }, []);
  async function checkTicket(ticket = { game, date, lines }) { setLoading(true); setError(''); setResult(null); try { const response = await fetch(`/api/pcso?date=${encodeURIComponent(ticket.date)}&game=${encodeURIComponent(ticket.game)}`); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'PCSO results could not be reached.'); setResult(data); } catch (err) { setError(err instanceof Error ? err.message : 'Please try again.'); } finally { setLoading(false); } }
  async function chooseImage(event: ChangeEvent<HTMLInputElement>) {
    const file = event.target.files?.[0]; if (!file) return; setImage(URL.createObjectURL(file)); setScanning(true);
    try {
      const { default: Tesseract } = await import('tesseract.js'); const { data } = await Tesseract.recognize(file, 'eng'); const ticket = scanTicket(data.text.replace(/\r/g, ''));
      if (ticket.detectedGame) setGame(ticket.detectedGame); if (ticket.date) setDate(ticket.date); if (ticket.entries) setLines(ticket.entries);
      if (!ticket.detectedGame || !ticket.date || ticket.entries.split('\n').length !== 4) throw new Error('I read part of the ticket, but not all four A–D entries. The detected details are kept below—complete only the missing entry, then check again.');
      await checkTicket({ game: ticket.detectedGame, date: ticket.date, lines: ticket.entries });
    } catch (scanError) { setError(scanError instanceof Error ? scanError.message : 'I could not read this ticket.'); } finally { setScanning(false); }
  }
  const winning = result ? numbers(result.combination) : [];
  const hasWinner = result && ticketLines.some((line) => numbers(line).filter((n) => winning.includes(n)).length === winning.length);
  return <main className="min-h-screen bg-[#071a2b] text-slate-950"><div className="mesh" /><div className="relative mx-auto max-w-6xl px-4 pb-12 pt-5 sm:px-7 sm:pt-8">
    <header className="mb-7 flex items-center justify-between text-white"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-[#f7c843] text-[#07243a]"><Ticket size={22}/></span><div><h1 className="text-xl font-black tracking-tight">Lotto Lens</h1><p className="text-xs text-sky-100/70">PCSO ticket checker</p></div></div><a href="https://www.pcso.gov.ph/searchlottoresult.aspx" target="_blank" rel="noreferrer" className="hidden items-center gap-1 rounded-full border border-white/20 px-3 py-2 text-xs font-semibold text-white sm:flex">PCSO results <ExternalLink size={13}/></a></header>
    <section className="grid gap-5 lg:grid-cols-[1.08fr_.92fr]"><div className="overflow-hidden rounded-[2rem] bg-white shadow-2xl shadow-black/20"><div className="border-b border-slate-100 px-5 py-5 sm:px-7"><p className="text-xs font-bold uppercase tracking-[.18em] text-sky-700">One ticket at a time</p><h2 className="mt-1 text-2xl font-black tracking-tight text-[#08243b]">Point, scan, see the result</h2><p className="mt-1 text-sm leading-6 text-slate-500">Take a clear photo. Lotto Lens reads the game, draw date, and only the four A–D number lines, then checks the official result automatically.</p></div><div className="grid gap-5 p-5 sm:p-7 md:grid-cols-2"><div><input ref={input} onChange={chooseImage} accept="image/*" capture="environment" className="hidden" type="file" id="ticket-photo"/><label htmlFor="ticket-photo" className="group flex min-h-60 cursor-pointer flex-col items-center justify-center overflow-hidden rounded-3xl border-2 border-dashed border-sky-200 bg-sky-50 p-4 text-center transition hover:border-sky-500 hover:bg-sky-100">{image ? <img src={image} alt="Selected lotto ticket" className="h-56 w-full rounded-2xl object-cover"/> : <><span className="mb-3 grid h-14 w-14 place-items-center rounded-2xl bg-[#0b6b93] text-white shadow-lg shadow-sky-900/15"><Camera size={26}/></span><span className="font-bold text-[#08243b]">Scan ticket</span><span className="mt-1 text-xs text-slate-500">Use your camera or upload a photo</span></>}</label>{(scanning || loading) && <p className="mt-2 flex items-center gap-1 text-xs font-bold text-sky-700"><ScanLine className="animate-pulse" size={14}/>{scanning ? 'Reading ticket details…' : 'Checking official PCSO result…'}</p>}{image && <button onClick={() => { setImage(null); if (input.current) input.current.value = ''; }} className="mt-2 flex items-center gap-1 text-xs font-semibold text-slate-500 hover:text-slate-900"><RotateCcw size={13}/> Scan another ticket</button>}<p className="mt-4 rounded-2xl bg-amber-50 px-3 py-3 text-xs leading-5 text-amber-900"><ShieldCheck className="mr-1 inline h-4 w-4"/>Only four complete entries are accepted: A:, B:, C:, and D:, each followed by six numbers.</p></div><div className="space-y-4"><label className="block text-sm font-bold text-slate-700">Detected lotto game<select value={game} onChange={(e) => setGame(e.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm font-medium outline-none focus:border-sky-600">{games.map((item) => <option key={item}>{item}</option>)}</select></label><label className="block text-sm font-bold text-slate-700">Detected draw date<input value={date} onChange={(e) => setDate(e.target.value)} type="date" className="mt-1.5 w-full rounded-xl border border-slate-200 px-3 py-3 text-sm outline-none focus:border-sky-600"/></label><label className="block text-sm font-bold text-slate-700">Detected A–D entries<textarea value={lines} onChange={(e) => setLines(e.target.value)} rows={4} placeholder={'A: 02 14 29 32 41 48\nB: 20 27 32 37 38 42'} className="mt-1.5 w-full resize-none rounded-xl border border-slate-200 px-3 py-3 font-mono text-sm outline-none focus:border-sky-600"/></label><p className="text-xs text-slate-500">Exactly four lettered entries are used. All other ticket text is ignored.</p><button disabled={!date || ticketLines.length !== 4 || loading} onClick={() => checkTicket()} className="flex w-full items-center justify-center gap-2 rounded-xl bg-[#f7c843] px-4 py-3.5 text-sm font-black text-[#08243b] shadow-lg shadow-amber-400/20 transition hover:bg-[#ffd65c] disabled:cursor-not-allowed disabled:opacity-45">{loading ? <><ScanLine className="animate-pulse" size={18}/>Checking PCSO result…</> : <>Check four entries <ChevronRight size={18}/></>}</button></div></div></div>
      <aside className="rounded-[2rem] bg-[#0b3150] p-5 text-white shadow-2xl shadow-black/20 sm:p-7">{!result && !error && <div className="flex h-full min-h-80 flex-col justify-center"><span className="mb-5 grid h-14 w-14 place-items-center rounded-2xl bg-white/10 text-[#f7c843]"><Trophy size={28}/></span><h2 className="text-2xl font-black">Ready to scan.</h2><p className="mt-3 max-w-sm text-sm leading-6 text-sky-100/75">Show a clear, front-on ticket to the camera. We’ll identify the draw and check each labelled entry automatically.</p></div>}{error && <div className="flex h-full min-h-80 flex-col justify-center"><XCircle className="mb-4 text-rose-300" size={34}/><h2 className="text-xl font-black">Couldn’t complete the automatic check</h2><p className="mt-3 text-sm leading-6 text-sky-100/75">{error}</p><p className="mt-3 text-xs leading-5 text-sky-100/60">You can correct the detected details and use “Check corrected details.”</p></div>}{result && <div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#f7c843]">Official PCSO draw found</p><h2 className="mt-2 text-xl font-black">{result.game}</h2><p className="mt-1 text-sm text-sky-100/70">{result.date} · Jackpot ₱{result.jackpot}</p><div className={`mt-5 rounded-2xl border p-4 ${hasWinner ? 'border-emerald-300/20 bg-emerald-400/10' : 'border-rose-300/20 bg-rose-400/10'}`}><p className="text-xs font-bold uppercase tracking-wide text-sky-100">Ticket result</p><p className="mt-1 text-lg font-black text-white">{hasWinner ? 'Winning line found' : 'No winning line'}</p><p className="mt-1 text-xs leading-5 text-sky-100/75">{hasWinner ? 'Check the line details and verify your prize with PCSO.' : 'No entry reached a winning prize tier.'}</p></div><div className="mt-5 rounded-2xl bg-white/10 p-4"><p className="text-xs font-bold uppercase tracking-wide text-sky-100/60">Winning combination</p><p className="mt-2 font-mono text-xl font-black tracking-wider text-[#f7c843]">{result.combination.replaceAll('-', ' · ')}</p></div><div className="mt-5 space-y-3">{ticketLines.map((line, index) => { const picked = numbers(line); const matches = picked.filter((n) => winning.includes(n)); const tier = prizeTier(game, matches.length, winning.length); const won = matches.length >= 3; return <div key={`${line}-${index}`} className="rounded-2xl bg-white p-4 text-[#08243b]"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wide text-slate-500">Entry {lineLabel(line, index)}</p><p className="mt-1 font-mono text-sm font-bold">{line.replace(/^\s*[A-Z]\s*[:.-]\s*/i, '')}</p></div>{won ? <CheckCircle2 className="text-emerald-600"/> : <XCircle className="text-slate-300"/>}</div><p className={`mt-3 text-sm font-black ${won ? 'text-emerald-700' : 'text-slate-600'}`}>{tier}</p><p className="mt-1 text-xs text-slate-500">Matched: {matches.length ? matches.join(', ') : 'none'}</p></div>; })}</div><a href="https://www.pcso.gov.ph/searchlottoresult.aspx" target="_blank" rel="noreferrer" className="mt-5 inline-flex items-center gap-1 text-xs font-bold text-[#f7c843] underline underline-offset-4">Verify on PCSO <ExternalLink size={13}/></a></div>}</aside></section>
    <footer className="mx-auto mt-6 max-w-4xl rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-center text-xs leading-5 text-sky-100/70">Lotto Lens is an independent helper, not PCSO. Results and prizes must be verified with the official PCSO draw results and your original ticket before claiming.</footer>
  </div></main>;
}
